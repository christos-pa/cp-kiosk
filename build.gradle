buildscript {
    repositories {
        maven { url uri("$rootDir/.gradle-local/repository") }
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.7.3"
    }
}
